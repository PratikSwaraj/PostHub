<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end">
            <a href="/logout" class="bg-red-500 px-3 px-2 rounded-md">Logout</a>
        </div>
        <h5>Edit your post.</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea name="content" placeholder="What's on your mind ?" class=" p-3 mt-3 w-1/3 resize-none bg-transparent border-2 border-zinc-800"> <%= post.content %> </textarea>
            <input type="submit" value="Update post" class="bg-yellow-500 text-white py-2 px-3 w-52 cursor-pointer block rounded-md">
        </form>
    </div>
</body>
</html>