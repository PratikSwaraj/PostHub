<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end gap-10">
            <a href="/posts/<%= user._id %>" class="bg-blue-500 px-3 px-2 rounded-md">All Posts</a>
            <a href="/logout" class="bg-red-500 px-3 px-2 rounded-md">Logout</a>
        </div>
        <h3 class="text-3xl mb-5"><span class="text-green-500">Hello,</span> <%= user.name %> </h3>
        <h5>You can create a new post !</h5>
        <form action="/post" method="post">
            <textarea name="content" placeholder="What's on your mind ?" class=" p-3 mt-3 w-1/3 resize-none bg-transparent border-2 border-zinc-800"></textarea>
             <input type="submit" value="Create new post" class="bg-blue-500 text-white py-2 px-3 w-52 cursor-pointer block rounded-md">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach((post) => { %>
                    <div class="post w-1/3 p-4 rounded-md mb-4 bg-zinc-800 border-2 border-zinc-700">
                        <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                        <p><%= post.content %></p>
                        <div class="btns flex mt-5 gap-5">
                            <a href="/edit/<%= post._id %>" class="text-zinc-500">edit</a>
                            <a href="/delete/<%= post._id %>" class="text-red-500">delete</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>